# 当前进度快照

记录日期：2026-06-24

项目状态：暂停开发，等待下一次继续实现。

当前分支：`main`

最新功能提交：

```text
09f98a2 实现地图选点地址反查
```

## 当前阶段

项目处于 V0.1 本地记录闭环开发阶段。

V0.1 的目标是先完成一个不依赖登录、不依赖云端、以 Room 本地数据库为核心的最小可用版本：

```text
新增记录 -> 本地保存 -> 时间轴查看 -> 详情查看 -> 编辑/删除 -> 备份导入导出
```

## 已实现功能

- Android Kotlin + Jetpack Compose 项目框架
- Room 本地数据库
- 首页概览统计
- 首页最近记录列表
- 首页最近记录跳转详情
- 新增记录
- 记录详情查看
- 记录编辑
- 编辑记录照片管理
- 记录删除与删除确认
- 发生日期选择
- 照片导入与详情展示
- 时间轴首图展示
- 时间轴卡片摘要优化
- 简单标签录入
- 标签展示
- 时间轴标签筛选
- 时间轴标签空状态提示
- 标签管理入口占位
- 标签管理列表
- 标签重命名、删除和颜色设置
- 时间轴搜索
- 时间轴搜索结果数量提示
- 地点名称录入
- 经纬度手动录入和校验
- 地图页点位数量和点位列表
- 地图选点入口占位
- 地图 SDK 选型文档
- 地图 SDK 接入准备
- 地图配置状态展示
- 高德地图 SDK 依赖接入
- 地图页真实地图展示
- 地图记录 marker 展示
- 地图 marker 点击查看详情
- 地图选点页面
- 地图选点地址反查
- 当前位置选择
- JSON 数据导出
- JSON 数据导入
- JSON 导入前预览
- 设置页本地优先开关持久化
- 开发过程记录文档
- GitHub 中文提交与推送流程

## 最近完成的模块

### 首页概览统计

首页从静态占位改为读取 Room 数据并展示：

- 记录数量
- 照片数量
- 标签数量
- 带坐标记录数量
- 最近一条记录

提交：

```text
38d3cc2 实现首页概览统计
```

### 地图选点入口占位

新增和编辑记录页已增加“地图选点”入口占位，点击后提示后续接入真实地图 SDK。

提交：

```text
baf2ac6 增加地图选点入口占位
```

### 地图 SDK 选型文档

已对比高德地图、Google Maps、MapLibre 和 OSM 官方瓦片，V0.2 推荐优先接入高德地图 Android SDK，并保留地图能力抽象边界。

提交：

```text
87f6663 补充地图 SDK 选型文档
```

### 地图 SDK 接入准备

已完成高德地图 Android SDK 接入前的工程准备，包括本地 Key 读取、Manifest 占位、基础网络权限、`BuildConfig` 注入和地图配置对象。

提交：

```text
8ca2d4d 准备地图 SDK 接入配置
```

### 地图配置状态展示

设置页已展示当前地图供应商和高德 Key 配置状态，不显示真实 Key 内容。

提交：

```text
6d54fda 实现地图配置状态展示
```

### 高德地图 SDK 依赖接入

已固定接入高德地图 Android SDK `10.0.600`，并在应用启动时调用 SDK 隐私合规初始化入口。当前仍未替换地图页 UI，也未申请定位权限。

提交：

```text
08abf16 接入高德地图 SDK 依赖
```

### 地图页真实地图展示

地图页已接入真实高德 `MapView` 容器。已配置高德 Key 时显示地图，未配置时显示清晰提示，并保留带坐标记录列表。

提交：

```text
fe48ca8 实现地图页真实地图展示
```

### 地图记录 marker 展示

地图页已将带坐标记录转换为高德地图 marker，marker 标题使用记录标题，摘要使用地点名称或经纬度，并保留下方点位列表。

提交：

```text
68907a5 实现地图记录 marker 展示
```

### 地图 marker 点击查看详情

地图 marker 已绑定记录 ID，点击 marker 后会导航到对应记录详情页。

提交：

```text
3084a6e 实现地图 marker 点击查看详情
```

### 地图选点页面

新增和编辑记录页的“地图选点”入口已接入真实选点页面，支持点击地图选择经纬度并回填表单。

提交：

```text
745a281 实现地图选点页面
```

### 地图选点地址反查

地图选点确认时会尝试通过高德 Web 逆地理编码接口反查地点名称，成功后同步回填地点输入框；失败时仍正常回填经纬度。

提交：

```text
09f98a2 实现地图选点地址反查
```

### 当前位置选择

地图选点页已支持“使用当前位置”。用户主动点击后请求前台定位权限，定位成功后设置当前坐标并尝试反查地点名称。

提交：

```text
待提交：实现当前位置选择
```

## 当前可验证路径

使用 Android Studio 打开项目目录：

```text
E:\岁迹
```

建议测试流程：

1. 启动 App。
2. 首页查看统计数据。
3. 点击首页最近记录进入详情页。
4. 新增记录，填写标题、正文、日期、地点、坐标、标签、照片。
5. 回到时间轴查看新记录。
6. 使用时间轴搜索和标签筛选。
7. 进入详情页，测试编辑和删除。
8. 打开地图页，查看地图区域或 Key 未配置提示；已配置 Key 时确认带坐标记录显示为 marker，并点击 marker 进入详情。
9. 在新增或编辑记录页点击“地图选点”，选择位置后确认回填经纬度；如果地址反查成功，地点也会自动回填。
10. 在地图选点页点击“使用当前位置”，授权后确认当前位置可用于回填。
11. 打开设置页，测试 JSON 导出。
12. 使用导出的 JSON 测试导入恢复。

命令行构建验证命令：

```powershell
& "$env:USERPROFILE\.gradle\wrapper\dists\gradle-8.11.1-bin\bpt9gzteqjrbo1mjrsomdt32c\gradle-8.11.1\bin\gradle.bat" :app:assembleDebug --no-daemon
```

最近一次功能构建结果：

```text
BUILD SUCCESSFUL
```

## 已知限制

- 地图页已有真实地图容器、记录 marker 和 marker 点击详情跳转。
- 地图选点已支持经纬度、地址反查和当前位置回填。
- JSON 导入没有差异预览。
- JSON 导入按原 ID 恢复，同 ID 本地数据会被覆盖。
- 照片备份目前只保存 URI 引用，不打包照片文件本体。
- 还没有系统化自动化测试和真机完整回归测试。

## 下一步建议

建议下一个独立功能模块：

```text
照片缓存增强
```

原因：

- 地图核心链路已经基本打通。
- 个人长期使用时，照片 URI 失效和大图加载是更实际的风险。
- 下一步应为照片生成 App 私有缩略图。

推荐实现边界：

- 导入照片后生成私有缩略图。
- 详情页和时间轴优先显示缩略图。
- 原始 URI 失效时展示缩略图或占位提示。
- 删除记录时清理对应缓存文件。

## 明天恢复开发时的第一步

1. 检查工作区：

```powershell
git status --short
```

2. 拉取远端：

```powershell
git pull
```

3. 运行构建：

```powershell
& "$env:USERPROFILE\.gradle\wrapper\dists\gradle-8.11.1-bin\bpt9gzteqjrbo1mjrsomdt32c\gradle-8.11.1\bin\gradle.bat" :app:assembleDebug --no-daemon
```

4. 从“照片缓存增强”模块继续。

## 本轮新增进度：照片私有缩略图缓存

当前正在补齐照片缓存增强模块：

- 新增 App 私有缩略图生成能力。
- 新增照片展示回退顺序：私有缩略图 -> 压缩图 -> 原始 URI。
- 新增编辑替换照片、删除记录时的缩略图清理。
- JSON 导入时不复用旧设备上的缩略图路径，避免恢复后图片路径失效。

提交计划：

```text
实现照片私有缩略图缓存
```

下一步建议：

```text
照片失效占位与重选提示
```

## 本轮新增进度：照片失效占位与重选提示

当前正在补齐照片失效时的可理解反馈：

- 时间轴首图加载失败时显示“照片无法显示”。
- 详情页照片加载失败时提示可编辑记录后重新选择。
- 图片加载中显示稳定占位，避免界面短暂空白。

提交计划：

```text
实现照片失效占位提示
```

下一步建议：

```text
照片完整备份包设计与导出准备
```

## 本轮新增进度：完整备份包导出准备

当前已新增 zip 备份包导出能力：

- 设置页新增“完整备份包”导出入口。
- 备份包包含 `lifeatlas_export.json`。
- 备份包包含 `backup_manifest.json`。
- 备份包会打包当前可读取的私有缩略图和压缩图缓存。
- 导出完成后显示记录数、照片引用数和媒体缓存文件数。

当前边界：

- 只支持 zip 导出，不支持 zip 导入恢复。
- 不复制系统相册原图，只打包 App 私有缓存媒体。

提交计划：

```text
实现完整备份包导出
```

下一步建议：

```text
完整备份包导入预览
```

## 本轮新增进度：完整备份包导入预览

当前已新增 zip 备份包预览能力：

- 数据导入入口支持选择 JSON 或 zip。
- zip 备份包会解析 `backup_manifest.json`。
- zip 备份包会解析 `lifeatlas_export.json`。
- 预览弹窗展示媒体缓存文件数量。
- zip 恢复按钮暂时禁用，避免未完成媒体恢复逻辑时误导入。

当前边界：

- JSON 备份仍支持预览和确认导入。
- zip 备份包目前只支持预览，不执行恢复。

提交计划：

```text
实现完整备份包导入预览
```

下一步建议：

```text
完整备份包媒体恢复
```

## 本轮新增进度：完整备份包媒体恢复

当前已接入 zip 备份包恢复：

- zip 预览后可以确认导入。
- 导入会恢复 Room 中的记录、照片引用、标签和标签关联。
- 导入会把备份包内媒体缓存写回 App 私有目录。
- 照片表会重写 `thumbnailPath` 和 `compressedPath`。
- 覆盖旧数据后会清理旧照片缓存文件。
- 如果数据库导入失败，会清理本次已恢复的媒体文件。

当前边界：

- 只恢复备份包内的 App 私有媒体缓存。
- 不主动复制或恢复旧设备系统相册原图。

提交计划：

```text
实现完整备份包媒体恢复
```

下一步建议：

```text
V1.0 回归测试清单与发布准备
```

## 本轮新增进度：版本信息与 V1.0 回归测试清单

当前已补齐发布准备的第一步：

- 设置页展示版本号、构建号、包名和本地优先状态。
- 关于岁迹展示当前阶段说明。
- 新增 `docs/V1.0回归测试清单.md`。
- 回归清单覆盖构建、首页、时间轴、新增、编辑、删除、照片、地图、导入导出和设置页。

当前边界：

- 当前是手动测试清单。
- 自动化测试和正式发布签名配置仍待接入。

提交计划：

```text
补充版本信息和回归测试清单
```

下一步建议：

```text
核心数据层自动化测试
```

## 本轮新增进度：核心数据层自动化测试

当前已新增第一组 Android instrumented 数据层测试：

- `LifeAtlasExportServiceTest`
- JSON 导出导入恢复结构化数据测试
- 完整备份包导入恢复媒体缓存路径测试
- Gradle 已配置 AndroidX Test、JUnit 和 Room Testing
- V1.0 回归测试清单已增加自动化测试命令

验证结果：

```text
:app:assembleDebug :app:assembleDebugAndroidTest
BUILD SUCCESSFUL
```

当前边界：

- 已验证测试包构建成功。
- 真正运行测试需要连接 Android 设备或启动模拟器。

提交计划：

```text
补充核心数据层自动化测试
```

下一步建议：

```text
数据库 Migration 自动化测试
```

## 本轮新增进度：数据库 Migration 自动化测试

当前已新增 Room Migration 测试：

- `AppDatabaseMigrationTest`
- 验证 `1 -> 2` 迁移保留记录并创建照片表
- 验证 `2 -> 3` 迁移保留记录和照片并创建标签表
- 验证 `1 -> 3` 连续迁移到最新 schema
- androidTest assets 已挂载 Room schema 目录

验证结果：

```text
:app:assembleDebug :app:assembleDebugAndroidTest
BUILD SUCCESSFUL
```

当前边界：

- 已验证测试包构建成功。
- 真正运行测试需要连接 Android 设备或启动模拟器。

提交计划：

```text
补充数据库迁移自动化测试
```

下一步建议：

```text
错误处理与异常提示打磨
```

## 本轮新增进度：设置页错误处理与异常提示打磨

当前已优化设置页导入导出提示：

- 成功、提示、错误消息分级展示。
- JSON 导出准备失败时不再卡在准备中。
- 损坏 zip、无效 JSON、文件读写失败会显示更明确的中文提示。
- 导出、备份包导出、导入预览、JSON 导入、zip 导入统一使用错误映射。

验证结果：

```text
:app:assembleDebug :app:assembleDebugAndroidTest
BUILD SUCCESSFUL
```

提交计划：

```text
优化设置页错误提示
```

下一步建议：

```text
编辑与照片选择异常提示打磨
```

## 本轮新增进度：新增与编辑记录异常提示打磨

当前已优化新增/编辑记录页提示：

- 保存失败时保留当前页面内容。
- 坐标错误提示更明确，并说明可以清空坐标后保存。
- 照片选择为空、重复选择、添加成功、移除成功都有提示。
- 地图选点回填后有成功提示。

验证结果：

```text
:app:assembleDebug :app:assembleDebugAndroidTest
BUILD SUCCESSFUL
```

提交计划：

```text
优化记录编辑异常提示
```

下一步建议：

```text
详情页删除失败兜底
```

## 本轮新增进度：详情页删除失败兜底

当前已优化记录详情页删除流程：

- 删除中禁用编辑和删除按钮。
- 删除失败时停留在详情页并展示错误提示。
- 删除成功后保持原有返回上一页流程。
- 防止重复点击触发多次删除。

验证结果：

```text
:app:assembleDebug :app:assembleDebugAndroidTest
BUILD SUCCESSFUL
```

提交计划：

```text
优化详情页删除失败提示
```

下一步建议：

```text
地图选点失败提示统一
```

## 本轮新增进度：地图选点失败提示统一

当前已优化地图选点页失败提示：

- 地址反查结果改为结构化结果。
- 区分地图 Key 未配置、网络不可用、服务不可用、未找到地址。
- 失败时仍保留经纬度，不阻断表单回填。
- 定位权限拒绝时明确提示仍可手动选点。

验证结果：

```text
:app:assembleDebug :app:assembleDebugAndroidTest
BUILD SUCCESSFUL
```

提交计划：

```text
统一地图选点失败提示
```

下一步建议：

```text
发布配置准备
```

## 本轮新增进度：发布配置准备

当前已补齐发布准备基础：

- release 构建可选读取本机签名配置。
- keystore 和签名文件已加入 `.gitignore`。
- 新增 `docs/发布准备清单.md`。
- README 文档目录加入发布准备清单。
- 发布清单覆盖版本策略、签名配置、地图 Key、隐私权限说明、构建命令和阻断项。

验证结果：

```text
:app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease
BUILD SUCCESSFUL
```

提交计划：

```text
补充发布配置准备
```

下一步建议：

```text
隐私政策正文草案
```

## 版本控制规则

后续继续遵循：

```text
一个独立功能模块 -> 构建验证 -> 更新文档 -> 中文提交 -> git push
```

如果 `git push` 不成功，停止开发下一个功能，先处理同步问题。
