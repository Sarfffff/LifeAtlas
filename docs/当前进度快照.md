# 当前进度快照

记录日期：2026-06-24

项目状态：暂停开发，等待下一次继续实现。

当前分支：`main`

最新功能提交：

```text
a7f692a 实现首页最近记录跳转
```

## 当前阶段

项目处于 V0.1 本地记录闭环开发阶段。

V0.1 的目标是先完成一个不依赖登录、不依赖云端、以 Room 本地数据库为核心的最小可用版本：

```text
新增记录 -> 本地保存 -> 时间轴查看 -> 详情查看 -> 编辑/删除 -> 备份导入导出
```

## 已实现功能

- Android Kotlin + Jetpack Compose 项目框架
- Room 本地数据库
- 首页概览统计
- 首页最近记录列表
- 首页最近记录跳转详情
- 新增记录
- 记录详情查看
- 记录编辑
- 编辑记录照片管理
- 记录删除与删除确认
- 发生日期选择
- 照片导入与详情展示
- 时间轴首图展示
- 时间轴卡片摘要优化
- 简单标签录入
- 标签展示
- 时间轴标签筛选
- 时间轴标签空状态提示
- 标签管理入口占位
- 标签管理列表
- 标签重命名、删除和颜色设置
- 时间轴搜索
- 时间轴搜索结果数量提示
- 地点名称录入
- 经纬度手动录入和校验
- 地图页点位数量和点位列表
- JSON 数据导出
- JSON 数据导入
- JSON 导入前预览
- 设置页本地优先开关持久化
- 开发过程记录文档
- GitHub 中文提交与推送流程

## 最近完成的模块

### 首页概览统计

首页从静态占位改为读取 Room 数据并展示：

- 记录数量
- 照片数量
- 标签数量
- 带坐标记录数量
- 最近一条记录

提交：

```text
38d3cc2 实现首页概览统计
```

### 完整标签管理

标签管理页支持查看、重命名、删除和颜色设置；标签颜色会在标签管理页、时间轴筛选和详情页展示。

提交：

```text
待提交：实现完整标签管理
```

## 当前可验证路径

使用 Android Studio 打开项目目录：

```text
E:\岁迹
```

建议测试流程：

1. 启动 App。
2. 首页查看统计数据。
3. 点击首页最近记录进入详情页。
4. 新增记录，填写标题、正文、日期、地点、坐标、标签、照片。
5. 回到时间轴查看新记录。
6. 使用时间轴搜索和标签筛选。
7. 进入详情页，测试编辑和删除。
8. 打开地图页，查看带坐标记录是否出现在点位列表。
9. 打开设置页，测试 JSON 导出。
10. 使用导出的 JSON 测试导入恢复。

命令行构建验证命令：

```powershell
& "$env:USERPROFILE\.gradle\wrapper\dists\gradle-8.11.1-bin\bpt9gzteqjrbo1mjrsomdt32c\gradle-8.11.1\bin\gradle.bat" :app:assembleDebug --no-daemon
```

最近一次功能构建结果：

```text
BUILD SUCCESSFUL
```

## 已知限制

- 地图页目前是点位列表，还没有接入真实地图 SDK。
- 经纬度目前需要手动填写，还没有地图选点和当前位置获取。
- JSON 导入没有差异预览。
- JSON 导入按原 ID 恢复，同 ID 本地数据会被覆盖。
- 照片备份目前只保存 URI 引用，不打包照片文件本体。
- 还没有系统化自动化测试和真机完整回归测试。

## 下一步建议

建议下一个独立功能模块：

```text
地图选点入口占位
```

原因：

- 当前地图页还是点位列表，新增/编辑记录仍需手动输入经纬度。
- 在新增和编辑页增加地图选点入口占位，可以为后续真实地图选点铺路。
- 先做入口说明，不接地图 SDK，风险较低。

推荐实现边界：

- 新增记录页坐标区域增加“地图选点”占位按钮。
- 编辑记录页坐标区域增加“地图选点”占位按钮。
- 点击后提示后续接入地图选点。
- 不接入真实地图 SDK。

## 明天恢复开发时的第一步

1. 检查工作区：

```powershell
git status --short
```

2. 拉取远端：

```powershell
git pull
```

3. 运行构建：

```powershell
& "$env:USERPROFILE\.gradle\wrapper\dists\gradle-8.11.1-bin\bpt9gzteqjrbo1mjrsomdt32c\gradle-8.11.1\bin\gradle.bat" :app:assembleDebug --no-daemon
```

4. 从“地图选点入口占位”模块继续。

## 版本控制规则

后续继续遵循：

```text
一个独立功能模块 -> 构建验证 -> 更新文档 -> 中文提交 -> git push
```

如果 `git push` 不成功，停止开发下一个功能，先处理同步问题。
